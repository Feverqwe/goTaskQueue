interface:
  display_name: "GoTaskQueue"
  short_description: "Run tasks and operate remote PTY sessions"
  default_prompt: "Use $gotaskqueue to find the right template, run the task, and follow its output."
